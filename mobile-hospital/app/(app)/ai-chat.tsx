import { ChatScreen } from '@/features/chat/ChatScreen';

/** Full-screen AI chat presented above tabs so closing returns to the previous tab. */
export default function AiChatModal() {
  return <ChatScreen />;
}
