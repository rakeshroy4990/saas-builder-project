# Legacy file kept for Dockerfile layer cache compatibility.
# Heavy/unused ML packages (torch, chromadb, langchain, sentence-transformers) were removed —
# they are not imported by this service and bloated the image + Cloud Run memory.
# App deps live in requirements.txt; optional Marker stack in requirements-marker.txt.
